// Deprecated — health check is now handled directly by monitoring.recordHealthSnapshot (cron).
// This module is kept as an empty placeholder to avoid breaking the Convex function registry.
export {};
